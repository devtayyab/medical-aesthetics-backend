diff --git a/frontend/src/components/molecules/TreatmentCard/TreatmentCard.tsx b/frontend/src/components/molecules/TreatmentCard/TreatmentCard.tsx
index c40368f..b8f8dea 100644
--- a/frontend/src/components/molecules/TreatmentCard/TreatmentCard.tsx
+++ b/frontend/src/components/molecules/TreatmentCard/TreatmentCard.tsx
@@ -6,11 +6,13 @@ import BotoxImg from "@/assets/Botox.jpg";
 export interface TreatmentCardProps {
     treatment: Treatment;
     onSelect?: (treatment: Treatment) => void;
+    onBookNow?: (treatment: Treatment) => void;
 }
 
 export const TreatmentCard: React.FC<TreatmentCardProps> = ({
     treatment,
     onSelect,
+    onBookNow,
 }) => {
     const handleClick = () => {
         onSelect?.(treatment);
@@ -46,9 +48,22 @@ export const TreatmentCard: React.FC<TreatmentCardProps> = ({
                             {treatment.shortDescription}
                         </p>
                     </div>
-                    <div className="text-right shrink-0">
-                        <p className="text-xs font-bold text-gray-400 uppercase tracking-tighter">Starting from</p>
-                        <p className="text-2xl font-black text-lime-700">┬ú{treatment.fromPrice || '?'}</p>
+                    <div className="text-right shrink-0 flex flex-col items-end gap-2">
+                        <div>
+                            <p className="text-xs font-bold text-gray-400 uppercase tracking-tighter">Starting from</p>
+                            <p className="text-2xl font-black text-lime-700">┬ú{treatment.fromPrice || '?'}</p>
+                        </div>
+                        {onBookNow && (
+                            <button
+                                className="px-4 py-2 bg-lime-600 text-white font-bold rounded-lg hover:bg-lime-700 transition shadow-sm text-sm"
+                                onClick={(e) => {
+                                    e.stopPropagation();
+                                    onBookNow(treatment);
+                                }}
+                            >
+                                Book Now
+                            </button>
+                        )}
                     </div>
                 </div>
 
diff --git a/frontend/src/pages/CRM/BookingModal.tsx b/frontend/src/pages/CRM/BookingModal.tsx
index a31b25c..f697631 100644
--- a/frontend/src/pages/CRM/BookingModal.tsx
+++ b/frontend/src/pages/CRM/BookingModal.tsx
@@ -13,9 +13,18 @@ interface BookingModalProps {
     onClose: () => void;
     initialDate?: Date;
     initialProviderId?: string;
+    initialClinicId?: string;
+    initialServiceId?: string;
 }
 
-export const BookingModal: React.FC<BookingModalProps> = ({ isOpen, onClose, initialDate, initialProviderId }) => {
+export const BookingModal: React.FC<BookingModalProps> = ({
+    isOpen,
+    onClose,
+    initialDate,
+    initialProviderId,
+    initialClinicId,
+    initialServiceId
+}) => {
     const dispatch = useDispatch<AppDispatch>();
 
     // Store states
@@ -26,8 +35,8 @@ export const BookingModal: React.FC<BookingModalProps> = ({ isOpen, onClose, ini
     // Form state
     const [formData, setFormData] = useState({
         clientId: '',
-        clinicId: '',
-        serviceId: '',
+        clinicId: initialClinicId || '',
+        serviceId: initialServiceId || '',
         providerId: initialProviderId === 'all' ? '' : (initialProviderId || ''),
         date: format(initialDate || new Date(), 'yyyy-MM-dd'),
         time: '10:00',
@@ -51,14 +60,16 @@ export const BookingModal: React.FC<BookingModalProps> = ({ isOpen, onClose, ini
 
     // Update time/date if initial changes
     useEffect(() => {
-        if (initialDate && isOpen) {
+        if (isOpen) {
             setFormData(prev => ({
                 ...prev,
-                date: format(initialDate, 'yyyy-MM-dd'),
-                providerId: initialProviderId === 'all' ? '' : (initialProviderId || '')
+                date: initialDate ? format(initialDate, 'yyyy-MM-dd') : prev.date,
+                providerId: initialProviderId === 'all' ? '' : (initialProviderId || prev.providerId),
+                clinicId: initialClinicId || prev.clinicId,
+                serviceId: initialServiceId || prev.serviceId,
             }));
         }
-    }, [initialDate, initialProviderId, isOpen]);
+    }, [initialDate, initialProviderId, initialClinicId, initialServiceId, isOpen]);
 
     if (!isOpen) return null;
 
diff --git a/frontend/src/pages/Client/Search.tsx b/frontend/src/pages/Client/Search.tsx
index be498c1..4339083 100644
--- a/frontend/src/pages/Client/Search.tsx
+++ b/frontend/src/pages/Client/Search.tsx
@@ -8,6 +8,8 @@ import { ClinicCard } from "@/components/molecules/ClinicCard/ClinicCard";
 import { TreatmentCard } from "@/components/molecules/TreatmentCard/TreatmentCard";
 import { Button } from "@/components/atoms/Button/Button";
 import { FaHospital, FaMap, FaList, FaStar, FaChevronDown, FaMapMarkedAlt } from "react-icons/fa";
+import { BookingModal } from "@/pages/CRM/BookingModal";
+import type { Treatment } from "@/types";
 
 export const Search: React.FC = () => {
   const dispatch = useDispatch<AppDispatch>();
@@ -20,6 +22,12 @@ export const Search: React.FC = () => {
   const [showDesktopMap, setShowDesktopMap] = useState<boolean>(true);
   const [showMobileMap, setShowMobileMap] = useState<boolean>(false);
 
+  const [isBookingModalOpen, setIsBookingModalOpen] = useState(false);
+  const [isClinicSelectionOpen, setIsClinicSelectionOpen] = useState(false);
+  const [selectedTreatment, setSelectedTreatment] = useState<Treatment | null>(null);
+  const [bookingClinicId, setBookingClinicId] = useState<string>('');
+  const [bookingServiceId, setBookingServiceId] = useState<string>('');
+
   const query = searchParams.get("q") || "";
   const location = searchParams.get("location") || "";
   const category = searchParams.get("category") || "";
@@ -53,6 +61,25 @@ export const Search: React.FC = () => {
     return "Any date";
   };
 
+  const handleBookNow = (treatment: Treatment) => {
+    if (!treatment.offerings || treatment.offerings.length === 0) {
+      alert("No clinics currently offer this treatment.");
+      return;
+    }
+
+    // Exactly 1 clinic offering it
+    if (treatment.offerings.length === 1) {
+      const offering = treatment.offerings[0];
+      setBookingClinicId(offering.clinic?.id || '');
+      setBookingServiceId(offering.id);
+      setIsBookingModalOpen(true);
+    } else {
+      // Multiple clinics
+      setSelectedTreatment(treatment);
+      setIsClinicSelectionOpen(true);
+    }
+  };
+
   return (
     <div className="min-h-screen bg-white">
       {/* Top Search Summary Header */}
@@ -147,6 +174,7 @@ export const Search: React.FC = () => {
                         key={treatment.id}
                         treatment={treatment}
                         onSelect={() => navigate(`/treatment/${treatment.id}`)}
+                        onBookNow={handleBookNow}
                       />
                     ))}
                   </div>
@@ -269,6 +297,50 @@ export const Search: React.FC = () => {
         </div>
       )}
 
+      {/* Clinic Selection Modal */}
+      {isClinicSelectionOpen && selectedTreatment && (
+        <div className="fixed inset-0 z-[80] flex items-center justify-center bg-gray-900/50 backdrop-blur-sm">
+          <div className="bg-white rounded-2xl shadow-xl w-full max-w-md overflow-hidden animate-in fade-in duration-200">
+            <div className="flex items-center justify-between px-6 py-4 border-b border-gray-100 bg-gray-50">
+              <h2 className="text-xl font-bold text-gray-900 truncate">Choose a Clinic</h2>
+              <button onClick={() => setIsClinicSelectionOpen(false)} className="text-gray-400 hover:text-gray-600 font-bold p-2 bg-gray-100 rounded-full">
+                Close
+              </button>
+            </div>
+            <div className="p-4 overflow-y-auto max-h-[60vh] space-y-3">
+              <p className="text-sm text-gray-500 mb-2">Select a clinic for <strong className="text-gray-900">{selectedTreatment.name}</strong></p>
+              {selectedTreatment.offerings?.map((offering: any) => (
+                <div
+                  key={offering.id}
+                  onClick={() => {
+                    setBookingClinicId(offering.clinic?.id || '');
+                    setBookingServiceId(offering.id);
+                    setIsClinicSelectionOpen(false);
+                    setIsBookingModalOpen(true);
+                  }}
+                  className="p-4 border border-gray-100 rounded-xl hover:border-lime-500 hover:shadow-md transition-all cursor-pointer flex justify-between items-center group"
+                >
+                  <div className="min-w-0">
+                    <h4 className="font-bold text-gray-900 group-hover:text-lime-700 truncate">{offering.clinic?.name || 'Unknown Clinic'}</h4>
+                  </div>
+                  <div className="text-right shrink-0 ml-4">
+                    <span className="font-black text-lime-700">┬ú{offering.price}</span>
+                  </div>
+                </div>
+              ))}
+            </div>
+          </div>
+        </div>
+      )}
+
+      {/* Booking Modal */}
+      <BookingModal
+        isOpen={isBookingModalOpen}
+        onClose={() => setIsBookingModalOpen(false)}
+        initialClinicId={bookingClinicId}
+        initialServiceId={bookingServiceId}
+      />
+
     </div>
   );
 };
diff --git a/frontend/src/pages/Client/TreatmentDetails.tsx b/frontend/src/pages/Client/TreatmentDetails.tsx
index 746d3cd..f46d16b 100644
--- a/frontend/src/pages/Client/TreatmentDetails.tsx
+++ b/frontend/src/pages/Client/TreatmentDetails.tsx
@@ -6,6 +6,7 @@ import { Button } from "@/components/atoms/Button/Button";
 import { css } from "@emotion/css";
 import { MapPin, ChevronRight, Info, Shield, Award } from "lucide-react";
 import BotoxImg from "@/assets/Botox.jpg";
+import { BookingModal } from "@/pages/CRM/BookingModal";
 
 const containerStyle = css`
   max-width: 1200px;
@@ -27,6 +28,11 @@ export const TreatmentDetails: React.FC = () => {
     const [treatment, setTreatment] = useState<Treatment & { offerings: any[] } | null>(null);
     const [isLoading, setIsLoading] = useState(true);
 
+    // Booking modal state
+    const [isBookingModalOpen, setIsBookingModalOpen] = useState(false);
+    const [bookingClinicId, setBookingClinicId] = useState<string>('');
+    const [bookingServiceId, setBookingServiceId] = useState<string>('');
+
     useEffect(() => {
         if (id) {
             // Since we don't have a specific redux action for this yet, we use API directly
@@ -131,7 +137,11 @@ export const TreatmentDetails: React.FC = () => {
                                             fullWidth
                                             variant="outline"
                                             className="h-10 rounded-xl text-xs font-black uppercase tracking-widest hover:!bg-black hover:!text-white transition-colors"
-                                            onClick={() => navigate(`/appointment/booking?clinicId=${offering.clinicId}&treatmentId=${treatment.id}`)}
+                                            onClick={() => {
+                                                setBookingClinicId(offering.clinicId);
+                                                setBookingServiceId(offering.id); // offering id is the serviceId
+                                                setIsBookingModalOpen(true);
+                                            }}
                                         >
                                             Book at this clinic
                                         </Button>
@@ -159,6 +169,14 @@ export const TreatmentDetails: React.FC = () => {
                     </div>
                 </div>
             </div>
+
+            {/* Booking Modal */}
+            <BookingModal
+                isOpen={isBookingModalOpen}
+                onClose={() => setIsBookingModalOpen(false)}
+                initialClinicId={bookingClinicId}
+                initialServiceId={bookingServiceId}
+            />
         </div>
     );
 };
diff --git a/frontend/src/types/index.ts b/frontend/src/types/index.ts
index fbff901..91c739d 100644
--- a/frontend/src/types/index.ts
+++ b/frontend/src/types/index.ts
@@ -57,6 +57,7 @@ export interface Treatment {
   fromPrice?: number;
   clinicsCount?: number;
   availableAt?: string[];
+  offerings?: any[];
 }
 
 export interface Service {
diff --git a/src/config/database.config.ts b/src/config/database.config.ts
index c2ece82..518c6fa 100644
--- a/src/config/database.config.ts
+++ b/src/config/database.config.ts
@@ -6,6 +6,7 @@ import { ConfigService } from '@nestjs/config';
 import { User } from '../modules/users/entities/user.entity';
 import { Clinic } from '../modules/clinics/entities/clinic.entity';
 import { Service } from '../modules/clinics/entities/service.entity';
+import { Treatment } from '../modules/clinics/entities/treatment.entity';
 import { Review } from '../modules/clinics/entities/review.entity';
 import { Lead, CrmAction, CustomerRecord } from '../modules/crm/entities';
 import { Tag } from '../modules/admin/entities/tag.entity';
@@ -42,6 +43,7 @@ export class DatabaseConfig implements TypeOrmOptionsFactory {
         User,
         Clinic,
         Service,
+        Treatment,
         Lead,
         CommunicationLog,
         CustomerRecord,
diff --git a/src/modules/clinics/clinics.module.ts b/src/modules/clinics/clinics.module.ts
index c1cfef5..11094de 100644
--- a/src/modules/clinics/clinics.module.ts
+++ b/src/modules/clinics/clinics.module.ts
@@ -5,6 +5,7 @@ import { ClinicManagementController } from './clinic-management.controller';
 import { ClinicsService } from './clinics.service';
 import { Clinic } from './entities/clinic.entity';
 import { Service } from './entities/service.entity';
+import { Treatment } from './entities/treatment.entity';
 import { Review } from './entities/review.entity';
 import { User } from '../users/entities/user.entity';
 import { Appointment } from '../bookings/entities/appointment.entity';
@@ -15,7 +16,7 @@ import { AgentClinicAccess } from '../crm/entities/agent-clinic-access.entity';
 
 @Module({
   imports: [
-    TypeOrmModule.forFeature([Clinic, Service, Review, User, Appointment, AgentClinicAccess]),
+    TypeOrmModule.forFeature([Clinic, Service, Treatment, Review, User, Appointment, AgentClinicAccess]),
     forwardRef(() => BookingsModule),
     forwardRef(() => LoyaltyModule),
     NotificationsModule,
diff --git a/src/modules/clinics/clinics.service.ts b/src/modules/clinics/clinics.service.ts
index c6eff44..3649bce 100644
--- a/src/modules/clinics/clinics.service.ts
+++ b/src/modules/clinics/clinics.service.ts
@@ -3,6 +3,8 @@ import {
   CreateClinicProfileDto,
   UpdateClinicProfileDto,
   AvailabilitySettingsDto,
+  CreateServiceDto,
+  UpdateServiceDto,
 } from './dto/clinic.dto';
 import { InjectRepository } from '@nestjs/typeorm';
 import { Clinic } from './entities/clinic.entity';
@@ -354,7 +356,7 @@ export class ClinicsService {
     });
   }
 
-  async createService(ownerId: string, serviceData: Partial<Service>): Promise<Service> {
+  async createService(ownerId: string, serviceData: CreateServiceDto): Promise<Service> {
     const clinic = await this.findByOwnerId(ownerId);
 
     // For management, we might need to find or create a treatment
@@ -380,7 +382,7 @@ export class ClinicsService {
       durationMinutes: serviceData.durationMinutes,
       clinicId: clinic.id,
       treatmentId: treatment?.id || (serviceData as any).treatmentId,
-      isActive: serviceData.isActive ?? true,
+      isActive: (serviceData as any).isActive ?? true,
       metadata: serviceData.metadata,
     });
 
@@ -395,19 +397,36 @@ export class ClinicsService {
   async updateService(
     ownerId: string,
     serviceId: string,
-    updateData: Partial<Service>,
+    updateData: UpdateServiceDto,
   ): Promise<Service> {
     const clinic = await this.findByOwnerId(ownerId);
 
     const service = await this.servicesRepository.findOne({
       where: { id: serviceId, clinicId: clinic.id },
+      relations: ['treatment'],
     });
 
     if (!service) {
       throw new NotFoundException('Service not found');
     }
 
-    Object.assign(service, updateData);
+    if (updateData.price !== undefined) service.price = updateData.price;
+    if (updateData.durationMinutes !== undefined) service.durationMinutes = updateData.durationMinutes;
+    if (updateData.isActive !== undefined) service.isActive = updateData.isActive;
+    if (updateData.metadata !== undefined) service.metadata = updateData.metadata;
+
+    if (service.treatment) {
+      let treatmentUpdated = false;
+      if (updateData.name !== undefined) { service.treatment.name = updateData.name; treatmentUpdated = true; }
+      if (updateData.shortDescription !== undefined) { service.treatment.shortDescription = updateData.shortDescription; treatmentUpdated = true; }
+      if (updateData.fullDescription !== undefined) { service.treatment.fullDescription = updateData.fullDescription; treatmentUpdated = true; }
+      if (updateData.category !== undefined) { service.treatment.category = updateData.category; treatmentUpdated = true; }
+      if (updateData.imageUrl !== undefined) { service.treatment.imageUrl = updateData.imageUrl; treatmentUpdated = true; }
+
+      if (treatmentUpdated) {
+        await this.treatmentsRepository.save(service.treatment);
+      }
+    }
 
     // Validate if service is active (published)
     if (service.isActive) {
